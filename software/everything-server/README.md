To setup a dev environment ensure you have virtualenv installed 

```sudo pip3 install virtualenv ```

then run the following

```virtualenv .env && source .env/bin/activate && pip install -r requirements.txt```
